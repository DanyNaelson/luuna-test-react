import React from 'react';
import { Container } from 'react-bootstrap';

const Repos = () => {
    return (
        <Container fluid>
            Repositorios
        </Container>
    );
};

export default Repos;